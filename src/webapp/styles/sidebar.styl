@require '_variables'

$sidebar-color = $color-grey-semi-light
$sidebar-max-width = 400px

.p-sidebar
  max-width $sidebar-max-width
  background-color $sidebar-color
  overflow-x hidden
  overflow-y scroll
  .p-logo
    margin 50px 30px 30px
  .p-external-components-list
    margin-top 50px
